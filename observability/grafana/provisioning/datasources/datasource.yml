apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # uid fixo: os painéis do dashboard referenciam este mesmo uid, então o
    # Grafana vincula tudo automaticamente, sem intervenção manual.
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
